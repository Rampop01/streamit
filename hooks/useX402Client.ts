// Deprecated: x402 payment is now handled via @stacks/connect wallet extension
// in PaymentGate.tsx. The x402-stacks middleware still runs server-side in
// app/api/content/[id]/route.ts for 402 responses.
